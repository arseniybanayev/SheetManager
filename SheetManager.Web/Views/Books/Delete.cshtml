@using SheetManager.Web.Controllers
@model SheetManager.Web.Models.Book

@{
    ViewData["Title"] = nameof(BooksController.Delete);
}

<h2>@nameof(BooksController.Delete)</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Book</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Path)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Path)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    
    <form asp-action="@nameof(BooksController.Delete)">
        <input type="hidden" asp-for="ID" />
        <input type="submit" value="@nameof(BooksController.Delete)" class="btn btn-default" /> |
        <a asp-action="@nameof(BooksController.Index)">Back to List</a>
    </form>
</div>
